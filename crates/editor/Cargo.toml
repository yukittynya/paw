[package]
name = "editor"
version = "0.1.0"
edition = "2024"

[dependencies]
buffer = { path = "../buffer/" }
crossterm = { version = "0.29.0", features = ["events"] }
thiserror = "2.0.12"
utils = { path = "../utils/" }
uuid = { version = "1.17.0", features = ["v4"] }
